name = "vasudev-chat"
main = "src/index.ts"
compatibility_date = "2026-01-15"
workers_dev = true

# Optional: bind a custom route once you've added a domain to Cloudflare.
# routes = [{ pattern = "agent.vasudev.xyz/*", custom_domain = true }]

[vars]
ALLOWED_ORIGINS = "https://vasudev.xyz,http://localhost:1313"
LANGFUSE_HOST = "https://cloud.langfuse.com"
LANGFUSE_CAPTURE_CONTENT = "true"
LANGFUSE_SAMPLE_RATE = "1"
OPENROUTER_MODEL = "nvidia/nemotron-3-nano-30b-a3b:free"
OPENROUTER_CONTROLLER_MODEL = "nvidia/nemotron-3-super-120b-a12b:free"

# Set with:
#   wrangler secret put OPENROUTER_API_KEY
#   wrangler secret put LANGFUSE_PUBLIC_KEY
#   wrangler secret put LANGFUSE_SECRET_KEY
# Tune model vars above without code changes. OPENROUTER_CONTROLLER_MODEL can be a smarter model for tool routing.
# (Get a free OpenRouter key at https://openrouter.ai/ — Nemotron free models are rate-limited.)
