source_up

# Wrangler prefers CLOUDFLARE_API_TOKEN over the OAuth login. The token from
# ~/.secrets.d/infra does not have Workers edit permissions for this account,
# while the local Wrangler OAuth login does. Keep the token available to other
# projects, but avoid it in this repo so `wrangler deploy` works.
unset CLOUDFLARE_API_TOKEN
